{% extends 'base.html.twig' %}

{% block title %} {% endblock %}

{% block body %}

    <h1>Liste des prochaines ventes aux enchères</h1>
    <table class="table">
    <thead>
    <tr>
        <th>Nom</th>
        <th>Date</th>
        <th>HeureDebut</th>
        <th>HeureFin</th>
        <th>Type</th>
        <th>Devise</th>
        <th>Voir les lots</th>
    </tr>
    </thead>
    <tbody>
    {% for vente_enchere in venteEnchere %}
    <tr>
        <td>{{ vente_enchere.nom }}</td>
        <td>{{ vente_enchere.date? vente_enchere.date|date('H:i:s') : '' }}</td>
        <td>{{ vente_enchere.heureDebut ? vente_enchere.heureDebut|date('H:i:s') : '' }}</td>
        <td>{{ vente_enchere.heureFin ? vente_enchere.heureFin|date('H:i:s') : '' }}</td>
        <td>{{ vente_enchere.type }}</td>
        <td>{{ vente_enchere.idDevise }}</td>
        <td>
            <a href="{{ path('afficher_vente_enchere_lots', {'id': vente_enchere.id}) }}">Lots</a>
        </td>


    </tr>
    {% endfor %}
    </tbody>
    </table>

{% endblock %}