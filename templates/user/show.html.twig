{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ user.age }}</td>
            </tr>
            <tr>
                <th>NumeroMobile</th>
                <td>{{ user.numeroMobile }}</td>
            </tr>
            <tr>
                <th>NumeroFixe</th>
                <td>{{ user.numeroFixe }}</td>
            </tr>
            <tr>
                <th>VerifSolvabilite</th>
                <td>{{ user.verifSolvabilite ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>VerifIdentite</th>
                <td>{{ user.verifIdentite ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>VerifRessortissant</th>
                <td>{{ user.verifRessortissant ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>EstCommissairePriseur</th>
                <td>{{ user.estCommissairePriseur ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>ListeMotCle</th>
                <td>{{ user.listeMotCle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
