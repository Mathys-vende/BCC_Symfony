{% extends 'base.html.twig' %}

{% block title %}CommissairePriseur{% endblock %}

{% block body %}
    <h1>CommissairePriseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commissaire_priseur.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commissaire_priseur_index') }}">back to list</a>

    <a href="{{ path('commissaire_priseur_edit', {'id': commissaire_priseur.id}) }}">edit</a>

    {{ include('commissaire_priseur/_delete_form.html.twig') }}
{% endblock %}
