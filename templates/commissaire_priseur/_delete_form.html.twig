<form method="post" action="{{ path('commissaire_priseur_delete', {'id': commissaire_priseur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commissaire_priseur.id) }}">
    <button class="btn">Delete</button>
</form>
