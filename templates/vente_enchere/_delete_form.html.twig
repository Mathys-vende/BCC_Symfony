<form method="post" action="{{ path('vente_enchere_delete', {'id': vente_enchere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vente_enchere.id) }}">
    <button class="btn">Delete</button>
</form>
